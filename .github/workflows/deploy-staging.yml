name: Deploy Staging to GitHub Pages

on:
  push:
    branches: [staging]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@v2
        with:
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          VITE_SUPABASE_URL: op://Pallaki Weddings/Pallaki Staging/supabase_url
          VITE_SUPABASE_ANON_KEY: op://Pallaki Weddings/Pallaki Staging/supabase_anon_key

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install
        working-directory: pallaki-app

      - name: Build
        run: npm run build
        working-directory: pallaki-app
        env:
          VITE_BASE_PATH: /staging/

      - name: Add 404.html for SPA routing
        run: cp pallaki-app/dist/index.html pallaki-app/dist/404.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: pallaki-app/dist
          destination_dir: staging
